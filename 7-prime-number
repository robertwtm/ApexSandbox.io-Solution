public Boolean isPrime(Integer num) {
    Boolean primo;
    for ( Integer i = 2; i < num ; i++){
        if( Math.mod(num,100)==0 )
            return primo = false;
    }
    return primo = true;
}
