public static Integer findLargest(List<Integer> nums) {
	//code here
    Integer maiorNumero = nums[0];

    for (Integer i : nums ) {
        
        if (nums[i] > largest) {
            maiorNumero = nums[i];
        }
    }
    return maiorNumero;
}