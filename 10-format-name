public String formatName(String firstName, String lastName) {
    String nomeCompleto 
    if (String.isBlank(firstName) || String.isBlank(lastName) != false){
    nomeCompleto = firstName +', '+lastName;
    return nomeCompleto;
    }
    
}